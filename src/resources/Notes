Notes

- 0-360 yaw code
	yaw = getHeadingDegs();
		
		if (yaw > 360) {
			while (yaw > 360) {
				yaw -= 360;
			}
		} else if (yaw < 0) {
			while (yaw < 0) {
				yaw += 360;
			}
		} 
		
		return yaw;